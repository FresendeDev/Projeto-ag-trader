import { BsPencil, BsFillTrashFill } from "react-icons/bs";
import styles from "./ProjectCard.module.css";

const ProjectCard = ({ id, nome, budge, category, handleRemove }) => {
  return (
    <div className={styles.teste}>
      <p>ProjectCard</p>
    </div>
  );
};

export default ProjectCard;
